# Design: Store Clock Injection

## Decision
Stores accept `clock: Clock | None = None` and default to `SystemClock`. This keeps the public constructor simple while allowing deterministic contract tests.

## Scope
This change covers timestamps generated by store state transitions. It does not alter producer-provided event timestamps.
